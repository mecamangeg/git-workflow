# Git Workflow Guardian - Production Dependencies

# Core dependencies
pyyaml>=6.0
requests>=2.28.0
watchdog>=3.0.0
python-dateutil>=2.8.0

# Phase 3: Dashboard & Analytics
flask>=2.3.0
flask-cors>=4.0.0

# Phase 3: Advanced notifications (optional)
PyQt5>=5.15.0  # Optional: Falls back to tkinter if not available
